import ITextObject from '../interfaces/ITextObject';

// Payment Methods Text -----------------------------------------------
const textPanelPaymentMethods: ITextObject = {
  card: 'card',
  cash: 'cash',
};

// Text Cart ---------------------------------------------------------
const textCart = {
  textPanelPaymentMethods,
};

export default textCart;
